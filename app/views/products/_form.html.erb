<%
=begin
%>
<%= simple_form_for @product, :html => { :multipart => true } do |f| %>
<%=# f.select(:category_id, options_for_select(categories), :prompt => "Select a category") %> 

<%= f.collection_select :category_id, @categories, :id, :name, prompt: "Select Category" %>

<%= f.input :name, label: "Product name" %>
<%= f.input :description %>
<%= f.input :price %>
<%= f.submit :submit, :class => 'btn btn-default' %>
<% end %>
<%
=end
%>

<div class="container">
  <%= form_with(model: @product, local: true) do |f| %>

  <%#= f.collection_select( :category_id, @categories, :id, :name ) %>
  <%= f.collection_select( :category_id, Category.all, :id, :name ) %>

    <div class="form-group">
      <%= f.label :product_name %>
      <%= f.text_field :name, class: "form-control" %>
    </div>


    <div class="form-group">
      <%= f.label :description %>
      <%= f.search_field :description, class: "form-control"%>
    </div>

    <div class="form-group ">
      <%= f.label :price %>
      <%= f.text_field :price, class: "form-control" %>
    </div>


    <div class="actions">
      <%= f.submit 'Submit', class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>


